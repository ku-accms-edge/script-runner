# =============================================================================
# Job カスタマイズ用パッチ
# =============================================================================
# リソース制限やその他のJob設定をカスタマイズする場合に使用します。
# =============================================================================

apiVersion: batch/v1
kind: Job
metadata:
  name: script-runner
spec:
  # リトライ回数
  # backoffLimit: 2

  # タイムアウト (10分)
  # activeDeadlineSeconds: 600

  template:
    spec:
      # initContainer のリソース制限
      initContainers:
        - name: git-clone
          resources:
            requests:
              memory: "64Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"

      # mainContainer のリソース制限
      containers:
        - name: script-runner
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "1000m"

          # 追加の環境変数
          # env:
          #   - name: PYTHONUNBUFFERED
          #     value: "1"
